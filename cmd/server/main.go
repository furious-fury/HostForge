// Package main is the HTTP API entrypoint (webhooks, WebSocket logs); stub until later phases.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "hostforge server: not implemented yet (Phase 4+)")
	os.Exit(1)
}
